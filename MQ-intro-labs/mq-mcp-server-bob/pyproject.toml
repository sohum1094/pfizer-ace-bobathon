[project]
name = "mq-mcp-server-bob"
version = "1.0.0"
description = "IBM MQ MCP Server for Bob - stdio transport version"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3.1.0",
    "httpx>=0.28.1",
    "mcp[cli]>=1.11.0",
]